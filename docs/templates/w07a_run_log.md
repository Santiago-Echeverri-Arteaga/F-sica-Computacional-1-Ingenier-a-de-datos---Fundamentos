# W06A — Run Log

## Comando usado
- OS: (Windows / macOS / Linux)
- Comando:
```bash
python -m src.pipeline.w06_pipeline --mode all
```
(o alternativa)
```bash
python src/pipeline/w06_pipeline.py --mode all
```

## Salida del pipeline (pegar aquí)
```
(paste stdout)
```

## Verificación artifacts
- `artifacts/gold_by_discoverymethod.csv` ✅
- `artifacts/gold_by_host.csv` ✅
