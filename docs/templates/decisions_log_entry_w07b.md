# Decisions Log — Entry (W06B)

- Fecha:
- Decisión:
- Razón:
- Alternativas:
- Evidencia (counts, EXPLAIN, timings):

Ejemplo:
- Decisión: Monitorear el tiempo de `dims` y fijar umbral (p.ej. < 5 s en mi máquina).
- Evidencia: `artifacts/w06b_stage_timings.csv` + conteos del `w06b_run_report.json`.
